using DataStructures

# Zygote.@adjoint! function heappush!(xs, x, ord)
#   heappush!(xs, x, ord), Δ -> begin
#     (nothing, nothing, nothing)
#   end
# end

# Zygote.@adjoint! function heappop!(xs, ord)
#   heappop!(xs, ord), Δ -> begin
#     (nothing, nothing)
#   end
# end
